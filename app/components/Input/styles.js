import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    inputStyle: {
        color: '#fff',
        fontSize: 18,
        lineHeight: 1,
        alignItems: 'center',
        width: 250,
        height: 50,
        borderWidth: 1,
        borderRadius: 40,
        borderColor: '#fff',
        paddingLeft: 40
    },

    containerStyle: {
        height: 50,
        alignItems: 'center',
        justifyContent: 'center',
    }
});
